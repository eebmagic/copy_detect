Check for _roster.json file
	if none make new _roster.json file

get input directory (and format input string)
check _roster for input directory

if not in roster:
	make all new signatures
	DATA = new signatures
	Save DATA & update _roster.json

if in roster:
	if complete match:
		DATA = json.load() from file referenced in roster
	if incomplete match:
		oldDATA = json.load() from file 
		remove dead signatures
		make signatures for new images
		(DATA = oldDATA - deadSigs + newSigs)
		Save DATA & update _roster.json

Run comparison of all signatures in DATA



Save DATA steps:
	
	convert DATA np.arrays  to lists (.tolist())
	make new time.json file for signatures
	update _roster.json with new time.json and new filenames
	remove old time.json file